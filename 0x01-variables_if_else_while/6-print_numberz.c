#include <stdio.h>
/**
 * main - prints all single digit numbers of base 10
 *
 * Return: Always 0 (Success)
 */
{
	int a;

	for (a = 48; a < 58; a++)
	{
		putchar(a);
	}
	putchar("\a");
	return (0);
}
