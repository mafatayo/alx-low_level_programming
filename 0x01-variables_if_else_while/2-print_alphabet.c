#include <stdio.h>
/**
 * main - prints the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ak;

	for (ak = 'a'; ak <= 'z'; ak++)
	{
		putchar(ak);
	}
	putchar('\n');
	return (0);
}
