#include <stdio.h>
/**
 * main - prints all single digit numbers of base
 *
 * Return Always 0 (Success)
 */
int main(void)
{
	int y;

	for (y = 0; y < 10; y++)
		printf("%d", y);
	printf("\y");
	return (0);
}
